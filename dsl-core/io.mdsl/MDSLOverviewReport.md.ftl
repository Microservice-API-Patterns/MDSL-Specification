# Report for MDSL specification ${filename}

## Endpoints 

The API description ${apiname} features the following endpoints: 

<#list serviceSpecification.contracts as endpoint>
* ${endpoint.name} 
</#list>
* No more endpoints found.
 