# Report for MDSL specification ${fileName}

## Endpoint Types 

The API description ${apiName} features the following endpoint types (a.k.a. service contracts): 

<#list serviceSpecification.contracts as endpoint_type>
* ${endpoint_type.name} 
</#list>
